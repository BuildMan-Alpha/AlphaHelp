<page>
    <sections>
        <section>
            <title>Example: Setting the Background of an Ink Control to an Image</title>
            <description>
                <p>The *[js:setBackground()]* method can be used to set the background of an Ink Control in the UX Component to an image. For example:</p>
            </description>
            <example code="js"><![CDATA[var url = "http://alphamediacapture.s3.amazona2s.com/4345.JPG";

// Get a pointer to the Ink Control Object:
var inkObj = {dialog.object}.getControl('INK1');

// define the Ink Background Properties:
var backgroundObj = {
    save: true,
    image: {
        data: url,
        size: 'cover'
    }
};

// set the Background of the 'INK1' control:
inkObj.setBackground(backgroundObj);]]></example>
        </section>
    </sections>
    <see>
        <ref link="how to set the background of an ink control">How to set the Background of an Ink Control</ref>
    </see>
</page>