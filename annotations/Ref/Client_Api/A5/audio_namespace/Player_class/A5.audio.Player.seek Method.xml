<page>
    <sections>
        <section>
            <title>Example: Seeking the AudioPlayer Control</title>
            <description>
                <p>The UX Component AudioPlayer control's JavaScript object can be used to control the audio playback on the client. The *[js:_play]* property of the object contains the audio player class methods. The JavaScript below demonstrates how to adjust the seek position for an AudioPlayer control:</p>
            </description>
            <example code="js"><![CDATA[// Get the object for the AudioPlayer Control:
var audioObj = {dialog.object}.getControl('AUDIOPLAYER');

if (audioObj) {
    // Set the playback location to 45 seconds from start:
    audioObj._play.seek(45);
}]]></example>
        </section>
    </sections>
    <see>
        <ref link="ux controls data controls more audioplayer">AudioPlayer Control</ref>
        <ref link="ux controls data controls more audio recorder and player control">AudioRecorderAndPlayer Control</ref>
    </see>
</page>