<page>
	<topic>Generic Utility Functions</topic>
	<description>Immediate if statement.</description>
	<syntax>$if(EXPRESSION as logical, VALUETRUE as any, VALUEFALSE as any)</syntax>
<sections>

<section>
<title>Example</title>
<example><![CDATA[var welcomeMsg = 'Hello ' + $if(sex=='Male','Mr.','Ms.') + $gvs('lastname');
/*The immediate if is more concise than using an if() statement, as shown below*/
var welcomeMsg = 'Hello ';
if(sex=='Male') {
    welcomeMsg = welcomeMsg + 'Mr.';
} else {
    welcomeMsg = welcomeMsg + 'Ms.';
}
welcomeMsg = welcomeMsg + $gvs('lastname'); ]]></example>	
	
</section>	
	
	
</sections>	
	
	
	
</page>