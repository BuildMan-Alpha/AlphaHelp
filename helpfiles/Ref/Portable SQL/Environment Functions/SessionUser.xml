<page api="portableSql">
    <shortlink>portablesql sessionuser function</shortlink>
    <topic  parent="" parentType="" elementName="SessionUser Portable SQL Function">SessionUser</topic>
    <description>
        <p>Get the current session user (according to the SQL database server).</p>
    </description>
    <prototypes>
        <prototype>SessionUser as C ()</prototype>
    </prototypes>
    <returns>
        <return>
            <name>result</name>
            <type>C</type>
            <description>
                <p>Returns the current session user.</p>
            </description>
        </return>
    </returns>
    <discussion>
        <p>The SessionUser function gets the current session user (according to the SQL database server).</p>
    </discussion>
    <example code="sql"  caption="AddHoursToDateTime() function executed on the Northwind MySQL database"><![CDATA[SELECT FIRST 1 SessionUser() AS Expr1 FROM Customers
=root]]></example>
</page>