<page api="js" build="1755">
    <shortlink>tbiObj getCurrentPaneLabel method</shortlink>
    <topic>tbiObj getCurrentPaneLabel Method</topic>
    <description>
        <p>Gets the current TabbedUI pane index.</p>
    </description>
    <prototypes>
        <prototype>var paneLabel = tbiObj.getCurrentPaneLabel();</prototype>
    </prototypes>
    <returns>
        <return>
            <name>paneLabel</name>
            <type>string</type>
            <description>
                <p>Returns the current pane's label.</p>
            </description>
        </return>
    </returns>
    <discussion>
        <p>The *[js:tbiObj.getCurrentPaneLabel()]* method gets the label for the current pane Tabbed UI pane.</p>
        <p>
            <example code="js"><![CDATA[var currentLabel = tbiObj.getCurrentPaneLabel();
alert("The current pane label is " + currentIndex);]]></example>
        </p>
    </discussion>
</page>