<page api="js" build="6960">
    <shortlink>tbiObj logout method</shortlink>
    <topic>tbiObj.logout Method</topic>
    <description>
        <p>Logs out the current user and redirects them to another page.</p>
    </description>
    <prototypes>
        <prototype>tbiObj.logout(targetURL);</prototype>
    </prototypes>
    <arguments>
        <argument>
            <name>targetURL</name>
            <type>string</type>
            <description>
                <p>The URL of the page where the browser should redirect.</p>
            </description>
        </argument>
    </arguments>
    <discussion>
        <p>The *[js:tbiObj.logout()]* method logs the current user out of the Alpha Security Framework and redirects them to another page.</p>
        <p>For example, assume that your application has a login page called "login.a5w". After the user logs in, the browser is redirected to the page that shows your TabbedUI component. One of the buttons shown in the TabbedUI is a logout button with the following code:</p>
        <p>
            <example code="js"><![CDATA[tbiObj.logout('login.a5w');]]></example>
        </p>
        <p>When the user clicks the button, they are logged out and redirected to the "login.a5w" page.</p>
    </discussion>
</page>