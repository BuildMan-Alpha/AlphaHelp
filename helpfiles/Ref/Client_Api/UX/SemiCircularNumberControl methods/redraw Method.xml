<page api="js" build="4199">
    <shortlink>ux semicircle method</shortlink>
    <topic>SemiCircularNumberDisplay redraw Method</topic>
    <description>Redraws the SemiCircularNumberDisplay control.</description>
    <prototype>numObj.redraw();</prototype>
    <discussion>The *[js.redraw()]* redraws the SemiCircularNumberDisplay control. If you change the control's minimum or maximum value, the *[js:.redraw()]* method must be called to update the display.</discussion>
    <example code="js"><![CDATA[//get a pointer to the control
var numObj = {dialog.object}.getControl('semicircularNumber_1');

if (numObj) {
    //set the min and max
    numObj.rgraphSettings.max = 200;
    numObj.rgraphSettings.min = 40;

    //redraw the control
    numObj.redraw();
}]]></example>
</page>