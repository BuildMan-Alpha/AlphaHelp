<page api="js">
    <shortlink>ux component duplicateRepeatingSectionRow Method</shortlink>
    <topic parent="{dialog.object}" parentType="object" elementName="duplicateRepeatingSectionRow Method">{dialog.object}.duplicateRepeatingSectionRow Method</topic>
    <description>Copies the values in the current Repeating Section row to a new row.</description>
    <prototype>{dialog.Object}.duplicateRepeatingSectionRow(repeatingSectionID [, flagFireEvents]);
    </prototype>
    <arguments>
        <argument>
            <name>repeatingSectionID</name>
            <type>string</type>
            <description>Row in repeating section to copy.</description>
        </argument>
        <argument optional="true">
            <name>flagFireEvents</name>
            <type>boolean</type>
            <description>
                <p>Default = true. A true/false value. By default, when the row is duplicated, onChange events in the target fields are fired. To suppress these events, pass in false to the *[js:flagFireEvents]* argument.</p>
            </description>
        </argument>
    </arguments>
    <discussion>
        <p>The *[js:duplicateRepeatingSectionRow()]* method copies the values from the active Repeating Section row to a new row. If a blank, unmodified row exists in the Repeating Section, the active row will be copied to the existing blank row. Otherwise, a new row will be added containing the duplicate data.</p>
    </discussion>
    <example code="js"><![CDATA[{dialog.object}.duplicateRepeatingSectionRow('REPSEC_1');]]></example>
    <see>
        <ref link="ux component methods">{dialog.object}</ref>
        <ref link="Duplicate and Copy Rows in a Repeating Section">Duplicate and Copy Rows in a Repeating Section</ref>
    </see>
    <terms>ux component,new row,repeating section,javascript functions</terms>
</page> 
