<page api="js" build="4331">
    <shortlink>ux component persistfs method</shortlink>
    <topic parent="{dialog.object}" parentType="object" elementName="_persistFS Method">{dialog.object}._persistFS Method</topic>
    <prototypes>
        <prototype>{dialog.object}._persistFS();</prototype>
    </prototypes>
    <returns>
        <return>
            <name>location</name>
            <type>string</type>
            <description>Returns the file system URL for the directory where List data sub-directory is saved.</description>
        </return>
    </returns>
    <description>Gets the file system URL of the directory in which the List data sub-directory is located.</description>
    <example code="js"><![CDATA[var fsLocation = {dialog.object}._persistFS();
var fileSystemFolder = {dialog.object}._persistFolder();

var fileAndURL = fsLocation + fileSystemFolder;
alert(fileAndURL);]]></example>
    <note>In the case where the component is running in AlphaLaunch, the base folder for the files is in a special folder returned by the  *[link:A5.shell.getAppFileStorageDir method]*. *[js:{dialog.object}._persistFS()]* will return the appropriate base folder name when the component is running in AlphaLaunch.</note>
    <terms>javascript functions</terms>
</page>