<page build="5824" api="js">
    <shortlink>ux stripCustomSchemeForAlphaWkWebView method</shortlink>
    <topic parent="{dialog.object}" parentType="object" elementName="stripCustomSchemeForAlphaWkWebView Method">{dialog.object}.stripCustomSchemeForAlphaWkWebView Method</topic>
    <description>Removes a custom scheme from a file URL.</description>
    <prototypes>
        <prototype>var result = {dialog.object}.stripCustomSchemeForAlphaWkWebView(fileURL)</prototype>
    </prototypes>
    <arguments>
        <argument>
            <name>fileURL</name>
            <type>string</type>
            <description>
                <p>The file URL for a local file that contains a custom scheme to remove.</p>
            </description>
        </argument>
    </arguments>
    <returns>
        <return>
            <name>result</name>
            <types>
                <type>string</type>
            </types>
            <description>
                <p>Returns the file URL with the custom scheme removed.</p>
            </description>
        </return>
    </returns>
    <discussion>
        <p>The *[js:{dialog.object}.stripCustomSchemeForAlphaWkWebView()]* method is used to remove a [custom scheme] from a file URL. The custom scheme is used to display files stored locally in an image, video, audio, etc tag on a mobile device in a PhoneGap application using the Alpha WkWebView plugin.</p>
        <p>
            <example code="js"><![CDATA[var ele = {dialog.object}.getPointer('image_1');
var fileURL = ele.src;
var fileURL2 = {dialog.object}.stripCustomSchemeForAlphaWkWebView(fileURL);]]></example>
        </p>
    </discussion>
    <limitations>PhoneGap Applications using Alpha WkWebView Only</limitations>
    <links>
        <link link="ux addCustomSchemeForAlphaWkWebView method">custom scheme</link>
    </links>
    <see>
        <ref link="ux addCustomSchemeForAlphaWkWebView method">{dialog.object}.addCustomSchemeForAlphaWkWebView Method</ref>
    </see>
</page>