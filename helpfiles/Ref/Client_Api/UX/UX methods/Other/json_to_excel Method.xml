<page api="js" build="0">
    <shortlink>ux component json_to_excel method</shortlink>
    <topic parent="{dialog.object}" parentType="object" elementName="json_to_excel Method"> {dialog.object}.json_to_excel Method</topic>
    <description>
        <p>Exports JSON data to Excel, then downloads the file to the client's machine.</p>
    </description>
    <prototypes>
        <prototype>{dialog.object}.json_to_excel(json)</prototype>
    </prototypes>
    <arguments>
        <argument>
            <name>json</name>
            <types>
                <type>Array of JSON Objects</type>
            </types>
            <description>
                <p>A JSON object to export to Excel. The JSON can either be an object containing an array of objects.</p>
            </description>
        </argument>
    </arguments>
    <discussion>
        <p>The *[js:{dialog.object}.json_to_excel()]* method generates an Excel file from JSON data. The JSON data is specified as array of objects where each object represents a record containing the same properties.</p>
    </discussion>
    <sections>
        <section>
            <title>Example</title>
            <example code="js"><![CDATA[var json = [
    {"firstname": "Fred", "lastname" : "Jones"},
    {"firstname": "Susan", "lastname" : "Smith"}
];

{dialog.object}.json_to_excel(json);]]></example>
        </section>
    </sections>
</page>