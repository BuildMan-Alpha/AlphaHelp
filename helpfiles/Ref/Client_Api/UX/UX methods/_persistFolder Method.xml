<page api="js" build="4331">
    <shortlink>ux component persistFolder method</shortlink>
    <topic parent="{dialog.object}" parentType="object" elementName="_persistFolder Method">{dialog.object}._persistFolder Method</topic>
    <prototypes>
        <prototype>{dialog.object}._persistFolder();</prototype>
    </prototypes>
    <returns>
        <return>
            <name>folderName</name>
            <type>string</type>
            <description>Returns the sub-folder where files are stored.</description>
        </return>
    </returns>
    <description>Gets the sub-folder in which the files are actually stored. Returned *[js:folderName]* includes a trailing / character.</description>
    <discussion>
        <p>For example, the fully qualified name of the file that contains all of the data in the List when the list is initially populated is therefore (assuming the List id is LIST1 and the UX Local Storage Namespace property was set to NS1):</p>
    </discussion>
    <example code="js"><![CDATA[var fsLocation = {dialog.object}._persistFS();
var fileSystemFolder = {dialog.object}._persistFolder();

var fullListFilename = fsLocation + fileSystemFolder + "NS1.LIST.LIST1";

alert(fullListFilename);]]></example>
    <terms>javascript functions</terms>
</page>