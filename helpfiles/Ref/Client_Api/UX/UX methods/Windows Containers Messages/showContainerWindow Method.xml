<page api="js">
    <shortlink>ux component showContainerWindow Method</shortlink>
    <topic parent="{dialog.object}" parentType="object" elementName="showContainerWindow Method">{dialog.object}.showContainerWindow Method</topic>

    <description>Shows a window defined by a 'container' control whose sub-type is 'window'. The controls in the 'window' container are the contents of the window.</description>

    <prototype>{dialog.Object}.showContainerWindow(ele,UXContainerWindow)</prototype>

    <arguments>
        <argument>
            <name>ele</name>
            <description>pointer to the element so that the window can be opened relative to this element (used only if the window position was specified to be 'relative' to some object.)</description>
        </argument>
        <argument>
            <name>UXContainerWindow</name>
            <description>name of the container (with sub-type of 'window'.)</description>
        </argument>
    </arguments>

    <discussion>Typically the window is opened by a button, in which case 'ele' can be set to 'this' - a pointer to the button that opens the window.</discussion>

    <example code="js"><![CDATA[//Code for a button that opens a container window
{dialog.Object}.showContainerWindow(this,'WINDOW1');]]></example>
    
    <see>
        <ref link="ux component methods">{dialog.object}</ref>
    </see>

        <terms>ux component,window,javascript functions</terms>
</page> 