<page>
    <topic>{dialog.object}.signatureIsEmpty Method</topic>
    <syntax>{dialog.Object}.signatureIsEmpty(UXControlName [, length]);</syntax>
    <description>Returns value indiciating whether or not the Signature control is empty.</description>
    <arguments>
        <argument>
            <name>UXControlName</name>
            <description>ID of the Signature Control</description>
        </argument>
        <argument>
            <name>length</name>
            <description>The "length" to compare the base64 encoded string to determine whether or not the signature is empty.</description>
        </argument>
    </arguments>
    <discussion>
        <p>Returns true if a signature control is empty (has not been signed). The base64 encoded signature is compared against a 'length' parameter to determine if the control is empty.</p>
        <p>If you do not pass in a *[js:length]* parameter, a default parameter value (appropriate for the type of browser you are using) is used.</p>
        <p>Note that even if a signature control has not been signed, the base64 encoded value in the control is not be empty.</p>
    </discussion>

    <example><![CDATA[var flagEmpty = {dialog.object}.signatureIsEmpty('signature1');]]></example>
</page>