<page api="js">
    <shortlink>list object searchModeOn method</shortlink>
    <topic>listObj.searchModeOn Method</topic>
    <description>If the List Search Part is set to 'QueryByForm' then turns search mode on.</description>
    <prototype>listObj.searchModeOn();</prototype>
    <example code="js"><![CDATA[var listObj = {dialog.object}.getControl("LIST1");

if (listObj) {
    listObj.searchModeOn();
}]]></example>
    <note>The search mode can be displayed by adding a Label control to the UX and setting the control's client-side calculated expression to *[js:dialog.listDetailView.LISTNAME.mode]* where 'LISTNAME' would be replaced by the actual List name (in capitals).</note>
    <limitations>List Control with Search Part</limitations>
    <see>
        <ref link="buildingdisconnectedapplications">List Control Overview</ref>
    </see>
</page>