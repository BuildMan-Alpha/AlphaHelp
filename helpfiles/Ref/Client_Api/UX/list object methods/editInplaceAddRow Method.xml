<page api="js" build="6232">
    <shortlink>editInPlaceAddRow method</shortlink>
    <topic>listObj.editInPlaceAddRow Method</topic>
    <description>
        <p>Add a new row to a List with in-place editing.</p>
    </description>
    <prototypes>
        <prototype>listObj.editInPlaceAddRow(rowNum)</prototype>
    </prototypes>
    <arguments>
        <argument>
            <name>rowNum</name>
            <type>number</type>
            <description>
                <p>Zero-based row number to add the new row. "0" corresponds to the first row in the list.</p>
            </description>
        </argument>
    </arguments>
    <discussion>
        <p>The *[js:editInPlaceAddRow()]* method adds a new record row below the specified row. The new row is automatically placed in edit mode.</p>
    </discussion>
    <example code="js"><![CDATA[var lObj = {dialog.object}.getControl('LIST1');
if (lObj) {
    // Add a new record at row 3.
    lObj.editInPlaceAddRow(2);
}]]></example>
    <limitations>List Control with In-place Editing Enabled</limitations>
    <see>
        <ref link="ux list inplace editing">List Control In-place Editing</ref>
    </see>
</page>