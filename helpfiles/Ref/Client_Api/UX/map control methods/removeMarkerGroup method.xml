<page api="js">
    <shortlink>map control removemarkergroup method</shortlink>
    <topic>mapControl.removeMarkerGroup Method</topic>
    <prototype>mapObj.removeMarkerGroup(groupName);</prototype>
    <arguments>
        <argument>
            <name>groupName</name>
            <type>string</type>
            <description>The name of the marker group containing the markers to remove.</description>
        </argument>
    </arguments>
    <description>Removes all markers in a group from a Map Control.</description>
    <example code="js"><![CDATA[//get a pointer to the map control for variable 'mymap1'
var mapObj = {dialog.object}.getControl('mymap1');

if (mapObj) {
    // remove the markers in 'myMarkerGroup1'
    mapObj.removeMarkerGroup('myMarkerGroup1');
}]]></example>
</page>