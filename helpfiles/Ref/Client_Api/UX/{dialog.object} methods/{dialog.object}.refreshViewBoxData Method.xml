<page>
   <topic>{dialog.object}.refreshViewBoxData Method</topic>

   <description>Refreshes a ViewBox control.</description>

   <syntax>{dialog.object}.refreshViewBoxData(UXViewBoxName);</syntax>
   <arguments>
     <argument>
         <name>UXViewBoxName</name>
         <description>The Id of the ViewBox control or a comma delimited list of ViewBox Ids to refresh.</description>
     </argument>
   </arguments>
   <discussion>
        <p>Refreshes a ViewBox control. If the ViewBox is based on a SQL query, a callback is made to re-execute the query. Contrast this with the <![CDATA[<viewBoxObject>.refresh()]]> method, which simply redraws the ViewBox using the existing ViewBox data.</p>
        <p>If you want to refresh ultiple ViewBoxes on a single Ajax callback, you can pass a comma delimited list of ViewBox Ids.</p>
    </discussion>

   <example><![CDATA[{dialog.object}.refreshViewBoxData('MYVIEWBOX1');

//refresh multiple ViewBoxes in the same ajax callback
{dialog.object}.refreshViewBoxData('MYVB1,MYVB2');]]></example>
</page> 