<page api="js">
    <shortlink>ux component fireLayoutRule Method</shortlink>
    <topic parent="{dialog.object}" parentType="object" elementName="fireLayoutRule Method">{dialog.object}.fireLayoutRule Method</topic>
    <description>Executes the actions for a Responsive Layout rule, ignoring whether or not the rule evaluates to a true result.</description>

    <prototype>{dialog.Object}.fireLayoutRule(ruleName)</prototype>

    <arguments>
        <argument>
            <name>ruleName</name>
            <description>The name of the rule that has the actions to execute.</description>
        </argument>
    </arguments>
    <discussion>The *[js:{dialog.object}.fireLayoutRule]* method executes the actions for a Responsive Layout rule, ignoring whether or not the rule evaluates to a true result. This method does not set the rule to the 'active' rule.</discussion>
    <example><![CDATA[{dialog.object}.fireLayoutRule('myrule1');]]></example>
    <terms>javascript functions</terms>
</page> 