<page api="js">
    <shortlink>grid component getParentObject method</shortlink>
    <topic parent="{grid.object}" parentType="object" elementName="getParentObject Method">{grid.object}.getParentObject Method</topic>
    <description>Returns a pointer to the parent object</description>
    <prototype>{grid.Object}.getParentObject()</prototype>
    <returns>
        <return>
            <name>result</name>
            <types>
                <type>object</type>
            </types>
            <description>
                <p>If the Grid component has a parent object, returns the parent object. If the Grid does not have a parent, returns *[js:false]*.</p>
            </description>
        </return>
    </returns>
    <discussion>
        <p>If this component was opened from a parent Grid or UX component, returns a pointer to the parent object so you can execute methods on the parent object.</p>
        <p>
            <example code="js"><![CDATA[var pObj = {grid.object}.getParentObject();
//If there is no a parent, 'pObj' will be false
if(pObj) {
    if(pObj.gridId) {
        // Parent is a Grid component
    }
    if(po.dialogId) {
        // Parent is a UX component
    }
}]]></example>
        </p>
    </discussion>
    <see>
        <ref link="grid%20components">Grid Component</ref>
        <ref link="Grid Component Properties and Methods">{grid.object}</ref>
    </see>
    <terms>grid component,components,object,parent grid,pointer</terms>
</page> 