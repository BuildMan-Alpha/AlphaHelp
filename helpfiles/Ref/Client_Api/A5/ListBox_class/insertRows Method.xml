<page api="js" generated="true">
	<shortlink>api client api a5 listbox insertrows method</shortlink>
	<topic parent="A5.ListBox" parentType="class" elementName="insertRows Method" >A5.ListBox.insertRows Method</topic>
	<prototype>A5.ListBox.insertRows(target,data)</prototype>
	<arguments>
		<argument>
			<name>target</name>
			<types><type>number</type><type>string</type><type>object</type></types>
			<description>The index or value in the list at which to insert the new rows. See *[link:A5.ListBox.getIndex]*.</description>
		</argument>
		<argument>
			<name>data</name>
			<type>object</type>
			<description>Data to insert into the list.</description>
		</argument>
	</arguments>
	<description> Insert new data into the list.</description>
	<discussion>
<p>
This method can be used to insert rows into the current data.
The rows will be inserted at the given index before order and filter operations are done.
If the data cannot be inserted because of an invalid index, the rows will be appended to the current data.
</p>
</discussion>
	<example code="js">
// To get a pointer to the A5.ListBox class see {dialog.object}.getControl
// assume lObj is a pointer to an instance of the A5.ListBox class
var data = [{name: 'Fred Smith', age: 23}];
lObj.insertRows(3,data); // the new row is insert as the 4th item in the list</example>
	<annotations>/Ref/Client_Api/A5/ListBox_class/A5.ListBox.insertRows Method.xml</annotations>
</page>
