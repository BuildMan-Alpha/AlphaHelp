<page>
	<topic>A5.ControlBar.refresh Method</topic>
	<prototype>A5.ControlBar.refresh([animation])</prototype>
	<arguments>
		<argument>
			<name>animation</name>
			<type>string|object:optional</type>
			<description>What animation to preform. If a string is passed in then it is considered the animation type (see below).</description>
		</argument>
		<argument>
			<name>type</name>
			<type>string</type>
			<description>The type of animation to do. There are three basic types. The "slide" type will slide the old control bar HTML out while the new HTML slides in. The "swap" type will slide the old HTML out before sliding the new HTML in from the same edge. The "fade" type will cross fade the new and old HTML. The "slide" and "swap" types are suffixed with either a specific edge "left", "right", "top" or "bottom", or with relative values based off the control bar edge "in", "out", "before" or "after". Values can be "slide-left", "slide-right", "slide-top", "slide-bottom", "slide-in", "slide-out", "slide-before", "slide-after", "swap-left", "swap-right", "swap-top", "swap-bottom", "swap-in", "swap-out", "swap-before", "swap-after" or "fade".</description>
		</argument>
		<argument>
			<name>duration</name>
			<type>number</type>
			<description>The duration in milliseconds that the animation should take.</description>
		</argument>
		<argument>
			<name>tween</name>
			<type>string</type>
			<description>The CSS "tween" for the animation. The default is "ease-in-out".</description>
		</argument>
	</arguments>
	<description> Refresh the control bar.</description>
</page>
