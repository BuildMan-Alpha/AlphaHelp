<page api="js" build="4280">
    <shortlink>api client api a5 shell getappfilestoragedir method</shortlink>
    <topic>A5.shell.getAppFileStorageDir Method</topic>
    <prototypes>
        <prototype>localDir = A5.shell.getAPpFileStorageDir();</prototype>
    </prototypes>
    <returns>
        <return>
            <name>localDir</name>
            <types>
                <type>String</type>
            </types>
            <description>Returns the file URL of the local directory where the App's data files are stored.</description>
        </return>
    </returns>
    <description>Gets the directory where the files for the application are stored.</description>
    <discussion>
        <p>The *[js:getAppFileStorageDir()]* method returns the directory on the device's file system where the data files for the App are stored. The directory is separate from other Apps on the device.</p>
    </discussion>
    <example code="js"><![CDATA[if (A5.shell) {
    var localDir = A5.shell.getAppFileStorageDir();
    alert(localDir);
}]]></example>
</page>