<page>
	
	<topic>json_extract()</topic>
	
	<description>json_extract() Function - Extracts a property from a JSON object. </description>

	<syntax>c text = json_extract( C json_text, c property_name)</syntax>

<discussion>
The JSON text passed to the function must be properly formed (i.e. use double quotes on property and string names). 
You can use json_reformat_safe() to 'clean' up the JSON string before calling json_extract.
</discussion>


	<example>
<![CDATA[dim json as c 
json = <<%txt%
{ 
    name: 'Fred',
    address: {
        street: '123 Main St',
        city: 'Boston',
        state: 'MA'
    }
}

'json_extract() requires properly formed JSON, so
'we first call json_reformat_safe()
dim json2 as c 
json2 = json_reformat_safe(json)
?json_extract(json2,"name")
= "Fred"
?json_extract(json2,"address")
= "
{
        street: '123 Main St',
        city: 'Boston',
        state: 'MA'
    }
"
%txt%

	]]></example>


	<see>
		<ref link="json_enumerate()">json_enumerate()</ref>
		<ref link="json_filter()">json_filter()</ref>
		<ref href="/Guides/Data Integration/JSON Data.xml">JSON Data</ref>
	</see>
	
	<terms>JSON,function,string,text</terms>
	
</page>