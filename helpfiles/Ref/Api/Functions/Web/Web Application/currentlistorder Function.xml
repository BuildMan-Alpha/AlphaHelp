<page source="Xbasicaddins/ajax_webcomponents.a5lib/scripts/currentlistorder.a5udf" generated="true">
    <shortlink>api current list order function source</shortlink>
    <topic>currentlistorder Function</topic>
    <description>Pseduo-function used to apply the server-side order for a List control in a UX Component to a Report.</description>
    <prototype>C currentListOrder(C listName)</prototype>
    <arguments>
        <argument>
            <name>listName</name>
            <type>C</type>
            <description>The List ID.</description>
        </argument>
    </arguments>
    <discussion>
        <p>The *[xb:currentListOrder()]* function is a pseudo-function that can be used to apply the current server-side order for a List to a Report. This allows you to generate a report using the same order as a List.</p>
        <p>To apply a List's filter expression to a report, use the [currentListFilter()] pseudo-function.</p>
    </discussion>
    <sections>
        <section>
            <important>*[xb:currentListOrder()]* only applies the server-side order for a List to the Report. It does not apply the client-side order.</important>
        </section>
    </sections>
    <limitations>Web Applications Only</limitations>
    <terms>web application,filter,function,list</terms>
    <links>
        <link link="api current list filter function source">currentListFilter()</link>
    </links>
    <see>
        <ref link="open a report, label or letter layout">Action Javascript: Open a Report, Label or Letter layout</ref>
        <ref link="api current list filter function source">currentListFilter()</ref>
        <ref link="api current grid filter function source">currentGridFilter()</ref>
        <ref link="api current grid order function source">currentGridOrder()</ref>
    </see>
</page>