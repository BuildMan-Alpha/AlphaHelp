<page source="Xbasicaddins/ajax_webcomponents.a5lib/scripts/currentlistfilter.a5udf" generated="true">
    <shortlink>api current list filter function source</shortlink>
    <topic>currentlistfilter Function</topic>
    <description>Pseduo-function used to apply the server-side filter for a List control in a UX Component to a Report.</description>
    <prototype>C currentListFilter(C listName)</prototype>
    <arguments>
        <argument>
            <name>listName</name>
            <type>C</type>
            <description>The List ID.</description>
        </argument>
    </arguments>
    <discussion>
        <p>The *[xb:currentListFilter()]* function is a pseudo-function that can be used to apply the current server-side filter for a List to a Report. This allows you to generate a report based on the records shown in a List.</p>
        <p>To apply a List's order expression to a report, use the [currentListOrder()] pseudo-function.</p>
    </discussion>
    <sections>
        <section>
            <important>*[xb:currentListFilter()]* only applies the server-side filter for a List to the Report. It does not apply the client-side filter.</important>
        </section>
    </sections>
    <limitations>Web Applications Only</limitations>
    <terms>web application,components,filter,function,list</terms>
    <links>
        <link link="api current list order function source">currentListOrder()</link>
    </links>
    <see>
        <ref link="api current list order function source">currentListOrder()</ref>
        <ref link="api current grid filter function source">currentGridFilter()</ref>
        <ref link="api current grid order function source">currentGridOrder()</ref>
    </see>
</page>