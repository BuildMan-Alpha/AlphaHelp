<page>
   <topic>DATE Function</topic>
    <description>Returns the current system date.</description>
   <syntax>Todays_Date as D = DATE()</syntax>
  
   <discussion>
DATE() returns today's date. The system date must be set at the DOS level (use the DOS DATE command). This function is useful for fields that should contain the current date.
   </discussion>
  
   <sections>
       <section>
       <title>Example</title>
            <description>Assume that you want to use a range filter in View mode to display only the records for overdue accounts. If your table stores the date that an order was placed in a field called ORDERDATE, the following expression yields the number of days since the order was placed:
</description>
            <example><![CDATA[date() -ORDERDATE]]></example>
       </section>
       
        <section>
            <description>You can generate a complete filter expression to find all open accounts over 30 days old by adding a check on a BALANCEDUE field. The filter is:
</description>
            <example><![CDATA[((date() - ORDERDATE) > 30) .AND. (BALANCEDUE > 0)]]></example>
       </section>
       
   </sections>
   <see>
      <ref href="/Ref/Api/Functions/Data Type/Date and Time Functions/index.xml">Date and Time Functions</ref>
   </see>
<terms>expressions,contains,display,field,filter,function,number,record,set,system,time</terms>
</page>