<page>
    <shortlink>ux listfieldimage image file transformation template property</shortlink>
    <topic>Image file transformation template</topic>
    <description>If the Image Filename is a relative filename, this transformation expression can be used to convert to an absolute filename.</description>
    <discussion>
        <p>If the image data contains an absolute filename (and therefore no 'Image file transformation template' setting is technically needed), specify the following 'Image file transformation template' :</p>
    </discussion>
    <example>
        <![CDATA[<absolute>\<value>]]>
    </example>
</page>
