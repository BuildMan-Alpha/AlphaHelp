<page build="5877">
    <shortlink>ux prop wrap password fields</shortlink>
    <topic parent="UX Advanced Properties" parentType="guide" elementName="Wrap password fields in form tags">Wrap password fields in form tags</topic>
    <description>
        <p>Wraps password fields in a &lt;form&gt; tag.</p>
    </description>
    <discussion>
        <p>Password fields in a UX component are automatically wrapped in &lt;form>...&lt;/form> tags to prevent Google Chrome from logging a warning in the console window. You can specify that you do not want password fields to be wrapped in &lt;form> tags. To do so, uncheck the *[ui:Wrap password fields in form tags]* property.</p>
        <p>When you change this property in a component that has password fields, you must recalculate all password fields in the component. This can be done by selecting the password controls and using the [Recalculate selected controls] tool in the UX component's *[ui:Menu]* dropdown.</p>
    </discussion>
    <links>
        <link link="recalculate%20selected%20controls">Recalculate selected controls</link>
    </links>
</page>