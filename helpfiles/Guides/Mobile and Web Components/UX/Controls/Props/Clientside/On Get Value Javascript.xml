<page build="4077">
    <shortlink>ux control ongetvaluejavascript prop</shortlink>
    <topic>On Get Value Javascript</topic>
    <description>An expression that transforms the value of the control when the *[js:.getValue()]* method is called.</description>
    <discussion>
        <p>The *[ui:On Get Value Javascript]* event fires when the {dialog.object}.getValue() method is called. This allows you to transform the value returned by the method.</p>
        <p>The JavaScript to transform the value can reference a variable called *[js:value]*. The JavaScript must return a value. For example:</p>
    </discussion>
    <example code="js"><![CDATA[return value.toUpperCase();]]></example>
</page>