<page>
    <shortlink>ux spin list properties height</shortlink>
    <topic>Height</topic>
    <description>The height of the Spin List control specified as either the number of values show above and below the current choice or an explicit value specified in CSS units (e.g. 30px).</description>
    <discussion>Specify the control height. You can enter a number (e.g. 1, 2, 3) or a dimension (e.g. *[css:30px]*). If you enter a number, this indicates the number of values to show above and below the selected value. If you enter a dimension, this indicates the total height of the control. The default Height is "*[ui:1]*"</discussion>
    <sections>
        <section>
            <title>Example: Height of "2"</title>
            <description>The Spin List shown below has a height of *[ui:2]*, meaning two values will be shown above and below the selected value:</description>
            <figure>
                <link>images/slh2.png</link>
            </figure>
        </section>
        <section>
            <title>Example: Height of "135px"</title>
            <description>
                <p>The Spin List shown below has a height of *[ui:135px]*:</p>
            </description>
            <figure>
                <link>images/slh135px.png</link>
            </figure>
        </section>
        <section>
            <title>Example: Changing the Height Dynamically at Run-time</title>
            <description>
                <p>You can change the height programmatically at run-time. For example:</p>
            </description>
            <example code="js"><![CDATA[var s = {dialog.object}.getControl('spin1');
s.height = 2; //set height to 2 entries above and below
s.refresh();

s.height = '100px'; //set height to 100px
s.refresh(); // refresh the Spin List control]]></example>
                <note>You must call the *[js:.refresh()]* method after changing the height property of a Spin List control.</note>
        </section>
    </sections>
</page>