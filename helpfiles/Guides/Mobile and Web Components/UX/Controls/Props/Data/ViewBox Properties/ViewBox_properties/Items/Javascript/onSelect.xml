<page>
     <shortlink>ux viewbox control properties items javascript on select</shortlink>
    <topic>onSelect</topic>
    <description>Fires when an item is selected.</description>
<sections>
    <section>
         <description>Your Javascript can references these variables: 'v' the value in the 'a5-value' attribute 
        on the template. 'ia', item arguments, for example a5-item="item1:arg1" ('item1' is the item name 
        and 'arg1' is item argument), 'i', the 'item' object (contains all of the properties for this item). 
        'ele', a pointer to the element on which the event occurred.
    </description>
    </section>
    <section>
        <description>If there are multiple items inside a viewbox that are attached different 
            onSelect events, then you can define a global [onSelectComplete] event that 
            will fire after every 'onSelect' event on each item has fired. 
        </description>
        
    </section>
    
</sections>
<links>
    <link link="UX viewbox properties global events on select complete">onSelectComplete</link>
    
</links>

<see>
       <ref link="ux viewbox control properties items javascript index">Viewbox properties 'Javascript' Events</ref>
    <ref link="UX viewbox properties global events on select">Global 'onSelect' event</ref>
 
    <ref link="UX viewbox properties global events on select complete">Global 'onSelectComplete' event.</ref>
     <ref link="ViewBoxControl">'ViewBox Control' guides</ref>
</see>

</page>