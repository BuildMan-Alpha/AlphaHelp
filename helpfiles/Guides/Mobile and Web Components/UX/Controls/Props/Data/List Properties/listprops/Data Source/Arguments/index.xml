<page>
    <shortlink>ux list data source arguments properties</shortlink>
    <topic>Arguments</topic>
    <description>Variables that can be used when querying the data source for the List. Arguments can be bound to page, session, and cookie variables.</description>
    <discussion>
        <p>*[ui:Arguments]* are server-side variables populated at run-time reading the value from a control, set using Xbasic in a server-side event, or populated from a page variable, session variable, or cookie. Arguments can be used in the List's SQL query to filter the data source.</p>
        <p>Arguments are defined using the *[ui:Define Arguments]* builder. Arguments defined in the List Builder are available to the entire UX Component and can also be accessed using the UX component's [Arguments] property. In addition to being available to the List, arguments can also be used to set the default value of a control or as part of a server-side event expression.</p>
        <p>Arguments can only be changed server-side using Xbasic, either with an [Ajax Callback] or [server-side event].</p>
    </discussion>
    <sections>
        <section>
            <description>
                <p>See [Arguments (UX Properties)] to learn more about creating and using Arguments in a UX Component.</p>
            </description>
        </section>
    </sections>
    <links>
        <link link="ux%20properties%20arguments">Arguments</link>
        <link link="ux%20properties%20arguments">Arguments (UX Properties)</link>
        <link link="ajax%20callback">Ajax Callback</link>
        <link link="ux%20component%20server%20side%20events">server-side event</link>
    </links>
</page>