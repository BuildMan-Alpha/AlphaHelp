<page>
    <shortlink>css layout container properties add spacing after last control on each row property</shortlink>
    <topic>Add spacing after last control on each row</topic>
   <description>Should the control spacing be added after the last column in a row (the row must be explicitly ended by setting the control's Break property to 'After').</description>
</page>
