<page>
    <shortlink>ux containers control group sub type</shortlink>
    <topic>ControlGroup Container</topic>
    <description>Controls in a *[ui:ControlGroup]* container are rendered using a mobile style.</description>
    <discussion>
        <p>Controls inside a *[ui:ControlGroup]* container are rendered using a style that is common on mobile applications. The *[ui:ControlGroup]* container is typically used in UX Components that users Panels.</p>
        <p>The style applied removes the border from the input controls and adds a divider line that separates each control.</p>
        <p>The ControlGroup container is not supported in applications using version 4 (e.g. 'Alpha' or themes that inherit from 'Alpha') or newer styles.</p>
    </discussion>
    <terms>container</terms>
</page>