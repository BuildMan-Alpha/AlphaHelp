<page>
<title>Simple Example Using Redis Keys</title>
<description>
Redis can be used to get/set keys/value pairs.
</description>
<sections>
<section>
<description>
<p>
Perhaps the simplest use of redist is storing key/value pairs.  This feature allows for variables that
can be saved and retreived across applications.
</p>
<p>
The Get method retreives a key value.  If a key does not yet exist, a type of 'Nil' will be returned in the RedisResult.
</p>
<p>
The Set method is used to set either a string or numeric value to a key.
</p>
<p>
The Del method is used to remove a key from redis.
</p>
</description>
<example><![CDATA[
dim redis as extension::RedisClient = extension::RedisClient::CreateClient()
dim key as extension::RedisResult
key = redis.Get("MyMessage")
? key.type
= "Nil"
? key.valueString
= ""


redis.Set("MyMessage","Hello world")
key = redis.Get("MyMessage")
? key.type
= "String"
? key.valueString
= "Hello world"

redis.Del("MyMessage")
key = redis.Get("MyMessage")
? key.type
= "Nil"
? key.valueString
= ""
]]>
</example>
</section>
</sections>
</page>