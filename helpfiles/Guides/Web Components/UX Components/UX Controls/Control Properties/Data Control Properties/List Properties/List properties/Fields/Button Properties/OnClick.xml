<page>
    <topic>OnClick</topic>
    <description>Specify the Javascript to be executed when the user clicks this button. The Javascript can reference 
        the 'rowNumber' variable (the zero based row number of the row for the button). 'listObj' (a pointer to the List object).
        'data' (a pointer to current row data values) and 'buttonThis' - a pointer to the button object that invoked the 
        action. To read data from a field in the current row use: data.fieldName where 'fieldName' is the case-sensitive 
        name of the field. e.g. data.firstName.
    </description>

<sections>
    <section>
        <title>Adding Javascript to a Button in a list</title>
        <steps>
            <step>
                <description>Click the OnClick property.</description>
                <figure><link>images/button8.png</link></figure>
            </step>
            <step>
                <description>Define the Javascript that you want to run when the button is clicked</description>
                <figure><link>images/button7.png</link></figure>
            </step>
            <step>
                <description>The component in Live Preview, after using the List Layout pane in the List Builder to place it in the component.</description>
                <figure><title>A button with a simple javascript alert event</title><link>images/button.png</link></figure>
            </step>
        </steps>
        
        

        
        
        
    </section>
    
    
    
</sections>

</page>