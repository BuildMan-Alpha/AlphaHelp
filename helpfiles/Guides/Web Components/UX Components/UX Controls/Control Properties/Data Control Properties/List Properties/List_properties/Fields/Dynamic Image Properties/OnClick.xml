<page>
    <topic>OnClick</topic>
    <description>Specify the Javascript to be executed when the user clicks this image.
        The Javascript can reference the 'rowNumber' variable (the zero based row number of the row for the 
        button). 'listObj' (a pointer to the List object') and 'data' (a pointer to current row data values).
        To read data from a field in the current row use: data.fieldName where 'fieldName' is the case-sensitive
        name of the field. e.g. data.firstName.
    </description>





</page>