<page>
    <topic>onSwipe</topic>
    <description>This defines a default onSwipe event that happens when an item is swipped, as on a 
        cell phone. 
    </description>
 <sections>
 
    <section>
         <description>If an onSwipe event is tied to a
        specific item's definition, then this localized event will be used
        in place of the default. The default onSwipe event only fires if an item 
        does NOT contain a onSwipe event in the item definition. 
         
        </description>
         
     </section>
</sections>
</page>