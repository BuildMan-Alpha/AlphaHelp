<page>
     <shortlink>ux viewbox control properties items javascript onbefore select</shortlink>
    <topic>onBeforeSelect</topic>
    <description>Fires after the html bound to an item is clicked on but before that item is actually selected. 
        If the selected item becomes [highlighted] when selected then this event will fire before the highlight is applied.
    </description>
<sections>
    <section>
         <description>Your Javascript can references these variables: 'v' the value in the 'a5-value' attribute 
        on the template. 'ia', item arguments, for example a5-item="item1:arg1" ('item1' is the item name 
        and 'arg1' is item argument), 'i', the 'item' object (contains all of the properties for this item). 
        'ele', a pointer to the element on which the event occurred.
    </description>
    </section>
    
</sections>
<links>
    <link link="selectable elements">highlighted</link>
</links>

<see>
       <ref link="ux viewbox control properties items javascript index">Viewbox properties 'Javascript' Events</ref>
    <ref link="UX viewbox properties global events on before select">Global 'onBeforeSelect' event</ref>
 
 <ref link="viewbox control">'ViewBox Control' guides</ref>
</see>

</page>