<page>
    <topic>Absolute Layout</topic>
    <description>Container for explicitly positioning controls.</description>
    <discussion>
        <p>Controls inside an *[ui:AbsoluteLayout]* container can be explicitly positioned relative to the top left corner of the container.</p>
        <p>Normally, controls in a UX component flow from left-to-right, top-to-bottom and are laid out automatically. Controls inside an *[ui:AbsoluteLayout]* container are positioned explicit x, y coordinates.</p>
        <p>You can display a bitmap image as the background for an *[ui:AbsoluteLayout]* container. A typical use case for the *[ui:AbsoluteLayout]* contaier is to display a bitmap image of a form and place controls - text boxes, checkboxes, etc - over the form's fields in the image.</p>
    </discussion>
    <terms>container</terms>
</page>