<page>
	<topic>.CONTAINER_GET() </topic>
	<syntax>Container as C = .CONTAINER_GET()</syntax>
	<description>Returns the name of the control's container.</description>
	<discussion>The .CONTAINER_GET() method returns the name of the control's container. The method returns NULL ("") unless the control is placed within a container within the form. The method is useful when using tabbed controls and frames.
Limitations, Desktop applications only.</discussion>
	<example><![CDATA[dim ptr as P
dim fld as P
ptr = form.load("customerx")
fld = ptr.child("Fullname")
? fld.Container_Get()  -> "TABBED1.tabs.sheet[2]" ]]></example>
	<see>
		<ref>Form Functions and Methods</ref>
		<ref>Browse Functions and Methods</ref>
		<ref>Form Control Methods</ref>
	</see>
	
</page>