<page>
   <topic>EVAL_VALID Function</topic>
   <syntax>Result_Flag as L = EVAL_VALID(C Expression[,C eval_context[,N session]])</syntax>
   <arguments>
     <argument>
         <name>Expression</name>
         <description>A quoted Xbasic statement. Character</description>
     </argument>
     <argument>
         <name>eval_context</name>
         <description>Optional.  The context in which the expression should be evaluated. This can be a list of tables in a CR-LF delimited list. It can also be the name of an open form (suffixed by ".this"), which would evaluate the expression in the context of the form.
</description>
     </argument>
     <argument>
         <name>session</name>
         <description>Numeric</description>
     </argument>
   </arguments>
   <description>Return True if the expression can be parsed (expression is properly formed), session context is optional.</description>
   <discussion>
EVAL_VALID() evaluates Expression and returns .T. (TRUE), if Expression is valid (can be interpreted as Xbasic code), or .F. (FALSE) if the Expression is invalid. This function is useful when the expression to be evaluated is itself constructed by another expression.

   </discussion>
  
   <sections>
       <section>
       <title>Example</title>
            <description>Assume that varA = 1, and varB = 2. Assume that theExpression = "varA + varB" :</description>
            <example><![CDATA[
eval_valid("1 + 2") -> .T.
eval_valid("varA + varB") -> .T.
eval_valid(theExpression) -> .T.
eval_valid("date() ")-> .T.
eval_valid("daaate() ")-> .F.
]]></example>
       </section>
       <section>
       <description>For more examples on how the Context parameter is used, see EVAL().</description>
       </section>
       
       
   </sections>
   <see>
      <ref>Expression Evaluation Functions</ref>
      <ref>EVAL()</ref>
   </see>
<terms>expressions,forms,function,list,session,xbasic</terms>
</page>