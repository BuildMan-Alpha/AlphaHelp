<page>
	<topic>SQL Genie</topic>
	<description>The SQL Genie creates SQL SELECT statements using Portable SQL.</description>
	<sections>
		<section>
			<cases>
				<case>
					<description>As you make selections in the SQL Genie, you will notice that it always displays the latest version of the SQL SELECT statement in the SQL control.</description>
				</case>
				<case>
					<description>You may directly edit the SQL statement in the SQL pane.</description>
				</case>
				<case>
					<description>Any time that your SQL SELECT statement changes, you may click Execute Query to show the first 100 records returned in the Preview pane.</description>
				</case>
				<case>
					<description>At any time you may click the History button to display SQL History dialog. This dialog allows you to revert to a previous version of your SQL SELECT statement.</description>
				</case>
			</cases>
		</section>
		<section>
			<description>There are six tabs in the genie.</description>
			<cases>
				<case>
					<title>Properties</title>
					<description>
						<p>Define GROUPBY clauses.</p>
						<p>Define DISTINCT clauses.</p>
						<p>Set record ranges.</p>
						<p>Define Arguments.</p>
					</description>
				</case>
				<case>
					<title>Tables</title>
					<description>
						<p>Select one or more tables.</p>
						<p>Define JOIN relationships between tables.</p>
					</description>
				</case>
				<case>
					<title>Columns</title>
					<description>Select fields from one or more tables.</description>
				</case>
				<case>
					<title>Filter</title>
					<description>Define WHERE clauses that filter the records that you will retrieve.</description>
				</case>
				<case>
					<title>Order</title>
					<description>Sequence the records that you will retrieve.</description>
				</case>
				<case>
					<title>Having</title>
					<description>Define HAVING clauses that filter the records that you will retrieve only when using a GROUP BY clause.</description>
				</case>
			</cases>
		</section>
		<section>
			<title>Starting the SQL Genie</title>
			<description>You can display the SQL Genie in the following ways:</description>
			<cases>
				<case>
					<description>In the Specify SQL SELECT Statement dialog click Query Builder... .</description>
				</case>
				<case>
					<description>In the Database Explorer, select a back-end database listing, right click, and select Open Query Builder.</description>
				</case>
			</cases>
		</section>
		<section>
			<title>SQL History</title>
			<description>
				<p>The SQL History dialog displays a list of the SQL SELECT statements that you have created. as such, it represents a list of snapshots since you began defining the query for the current report.</p>
				<p>To return to a previous version of the SQL SELECT statement, pick it from the list and click Select.</p>
			</description>
		</section>	
		<section>
			<title>Native Syntax</title>
			<description>The Native Syntax button displays the SQL Native Syntax dialog box. This dialog displays both the Portable SQL that has been generated by the SQL Genie and the native SQL version supported by any of these database products. The Copy to Clipboard button allows you to copy the native SQL for reuse in another context.</description>
		</section>	
	</sections>	
	<see>
		<ref href="/documentation/index?search=Getting Started with Enterprise Reporting">Enterprise Reporting</ref>
	</see>
</page>