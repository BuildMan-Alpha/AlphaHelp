<page>
   <topic>OBJECT.QUERYRUN Function</topic>
   <syntax><![CDATA[Record_Count as N = <OBJECT>.Queryrun([c filter_expression [,c order_expression [,c query_name [,c additive [,c level_name [,l displaywarning [,c query_options ]]]]]]])]]></syntax>
   <arguments>
   <argument>
         <name>Record_Count</name>
         <description>The number of records found by the query.
</description>
     </argument>
     <argument>
         <name>filter_expression</name>
         <description>An expression that selects records from the table.
</description>
     </argument>
     <argument>
         <name>order_expression</name>
         <description>Optional. Default = Record order. An expression that orders the records selected.
</description>
     </argument>
     <argument>
         <name>query_name</name>
         <description>Optional. The name of the query. Use "Temporary Query" to define a query that Alpha Anywhere can discard to make room for new queries.
</description>
     </argument>
     <argument>
         <name>additive</name>
         <description>Optional. Default = "No". "Yes" = The query is in addition to the form's current query.
"No" = The query replaces the form's currency query.
</description>
     </argument>
     <argument>
         <name>level_name</name>
         <description>Optional. Default = "crosslevel". The name of the parent table when the Filter_Expression is based on the child table or the parent and child tables where there is a one-to-many relationship between the parent and child tables.
</description>
     </argument>
     <argument>
         <name>displaywarning</name>
         <description>Optional. Default = .F. (FALSE). If no records are found, whether to display a warning.
</description>
     </argument>
     <argument>
         <name>query_options</name>
         <description><![CDATA[Optional. Default = "". Refer to <TBL>.QUERY_CREATE().]]>
</description>
     </argument>
   </arguments>
   <description>method</description>
   <discussion><![CDATA[
The <OBJECT>.QUERYRUN() method applies to:]]>
   </discussion>
  <sections>
       <section>
<list>
<item>
<name><![CDATA[Browses (for <OBJECT> use either the <BROWSE> pointer or the name of the browse)]]></name>
</item>
<item>
<name><![CDATA[Forms (for <OBJECT> use either the <FORM> pointer or the name of the form)]]></name>
</item>

</list>

       </section>
        <section>
            <description><![CDATA[The <OBJECT>.QUERYRUN() method applies a query to the current layout. The method returns the number of records selected by the query.
Limitations: Desktop applications only.]]>
</description>
          
       </section>
       
       
   </sections>
   <sections>
       <section><title>Example</title>
            <description>The current script opens a form and only displays records from Massachusetts:</description>
            <example><![CDATA[
dim P as P
P = form.load("customer information")
Result = p.QueryRun("bill_state_region = 'ma'")
if (result = 0) then
    ui_msg_box("Error","No records found")
    p.close()
else
    p.activate()
end if.
]]></example>
       </section>
       
        <section>
            <description>To modify the above script to sort the result by lastname, change the .QUERYRUN() method to:</description>
            <example><![CDATA[
Result = p.QueryRun("bill_state_region = 'ma'", "lastname")
]]></example>
       </section>
       
        <section>
            <description>The following script applies a filter additively. The script is attached to a button on a form:</description>
            <example><![CDATA[
Old_filter = topparent.filter_get()
Topparent.QueryRun("lastname = 'smith'", "", "", "Yes")
'To "undo" the last query:
Topparent.QueryRun(old_filter)
]]></example>
       </section>
   </sections>
   <see>
      <ref>Form Functions and  Methods</ref>
      <ref>Browse  Functions and Methods</ref>
   </see>
  
</page>