<page>
   <topic>OBJECT.VARIABLES Function</topic>
   <syntax>.Variables([C variable_frame_context])</syntax>
   <arguments>
     <argument>
         <name>variable_frame_context</name>
         <description>Character</description>
     </argument>
   </arguments>
   <description>Returns a pointer to the variables defined at the A5 level - optional flag for 'local,shared and global'.</description>
   <discussion>
      ...put discussion here....
   </discussion>
   <example><![CDATA[
...put example code here....
]]></example>
   <sections>
       <section>
            <description>put description here</description>
            <example><![CDATA[
      ...put example here....
]]></example>
       </section>
   </sections>
   <see>
      <ref>Form Functions and  Methods</ref>
      <ref>Browse  Functions and Methods</ref>
      <ref>GLOBAL_VARIABLES()</ref>
      <ref>SESSION_VARIABLES()</ref>
   </see>
   <original><![CDATA[&ltOBJECT>.VARIABLES() 
Syntax
Variable_List as P = &ltOBJECT>.VARIABLES( Frame_Context as C )
ArgumentDescription
Frame_ContextOptional. Default = . Determines the scope of the variables returned. The following values are possible.

 "Local"

 "Shared"

 "Global" 

Description
The &ltOBJECT>.VARIABLES() method applies to:

 Alpha Anywhere (for &ltOBJECT> use the keyword "A5")

 Control Panel (for &ltOBJECT> use the keyword "CONTROLPANEL". 
The &ltOBJECT>.VARIABLES() method returns a pointer to the dot variable Variable_List that contains the values of the object's variables.
Note : This method only works for forms at design time. If you want to get global variables at runtime, use vGlobalFrame = global_variables(). If you want to get session variables at runtime, use vSessionFrame = session_variables(topparent.SessionHandle()).
Limitations
Desktop applications only.
Example
dim ptr as P
ptr = ControlPanel.Variables()
? ptr.A_DB_CURRENT_PATH
= "C:\Program Files\A5V5\"

See Also
Form Functions and  Methods, Browse  Functions and Methods, GLOBAL_VARIABLES(), SESSION_VARIABLES()]]></original>
</page>