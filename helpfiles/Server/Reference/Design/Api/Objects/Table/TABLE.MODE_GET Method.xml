<page>
   <topic>table.MODE_GET Function</topic>
   <syntax>N Mode_Get()</syntax>
   <description>Get the data entry mode for the table.</description>
   <discussion>
      ...put discussion here....
   </discussion>
   <example><![CDATA[
...put example code here....
]]></example>
   <sections>
       <section>
            <description>put description here</description>
            <example><![CDATA[
      ...put example here....
]]></example>
       </section>
   </sections>
   <see>
      <ref>Table Functions and  Methods</ref>
   </see>
   <original><![CDATA[&ltTBL>.MODE_GET()
Syntax
Data_Entry_Mode as N = &ltTBL>.MODE_GET()
Description
The &ltTBL>.MODE_GET() method returns a number that indicates the current Data_Entry_Mode for the table. The possible return values are:
Return ValueModeData Entry Mode
0ViewNo data entry is currently taking place.
1ChangeA Change operation is underway.
2EnterAn Enter operation is underway.

Example
This script can be attached to an OK button on a dialog to make sure the current records is saved before the dialog is closed.
dim tbl as P
tbl = table.current()
if tbl.mode_get()> 0 then
   parent.commit()
end if
parent.close()

This script tells you which mode you are in.
dim tbl as P
tbl = table.current()
if tbl.mode_get()= 1 then
    ui_msg_box("Mode", "Change mode")
elseif tbl.mode_get()= 2 then
    ui_msg_box("Mode", "Enter mode")
else
    ui_msg_box("Mode", "View mode")
end if

See Also
Table Functions and  Methods]]></original>
</page>