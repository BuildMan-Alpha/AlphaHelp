<page>
   <topic>TABLE.REMVE_RECS_IN_LIST Function</topic>
   <syntax>C table.remve_recs_in_list(C primary_table ,C remove_list ,C primary_table_key [,C transaction_table_key [,L silent ]])</syntax>
   <arguments>
     <argument>
         <name>primary_table</name>
         <description>Character</description>
     </argument>
     <argument>
         <name>remove_list</name>
         <description>Character</description>
     </argument>
     <argument>
         <name>primary_table_key</name>
         <description>Character</description>
     </argument>
     <argument>
         <name>transaction_table_key</name>
         <description>Character</description>
     </argument>
     <argument>
         <name>silent</name>
         <description>Logical</description>
     </argument>
   </arguments>
   <description>Removes the records Remove_List from the Primary_table. Records are matched on the Primary_table_key</description>
   <discussion>
      ...put discussion here....
   </discussion>
   <example><![CDATA[
...put example code here....
]]></example>
   <sections>
       <section>
            <description>put description here</description>
            <example><![CDATA[
      ...put example here....
]]></example>
       </section>
   </sections>
   <see>
      <ref>Table Functions and  Methods</ref>
      <ref><TBL>.SUBTRACT()</ref>
   </see>
   <original><![CDATA[TABLE.REMVE_RECS_IN_LIST()
Syntax
TABLE.REMVE_RECS_IN_LIST( Primary_Table as C, Remove_List as C, Primary_Key as C [, Transaction_Table_Key as C [, Silent as L ] )
ArgumentDescription
Primary_TableThe name of the table that will have records removed from it.
Remove_ListThe name of the table containing the matching records to remove from the Primary_Table.
Primary_KeyThe expression used to compute key values in the Primary_Table.
Transaction_Table_KeyOptional. Default = Use the Primary_Key expression to compute key values. An expression (different from the Primary_Key expression ) to use to calculate the key values in the Remove_List table.
SilentOptional.

.T. = Do not display a confirmation message before proceeding.

.F. = Display a confirmation message first. 

Description
The TABLE.REMVE_RECS_IN_LIST() method removes all of the records contained in the Remove_List table from the records in the Primary_Table. ( Note : Contrast this function with the <TBL>.SUBTRACT() operation which would create a new table with the Primary_Table records less the Remove_List records.)
Alpha Anywhere determines which records from the Primary_Table to remove as follows. For each record in the Remove_List, Alpha Anywhere computes a key value, using the Transaction_Table_Key expression. If a corresponding key value (computed using the Primary_Key expression) is found, the record is removed.
See Also
Table Functions and  Methods, <TBL>.SUBTRACT()]]></original>
</page>