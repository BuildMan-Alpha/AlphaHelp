<page>
   <topic>table.INDEX_ADD Function</topic>
   <syntax>V Index_Add(C Tagname,C Order_expression[,C Filter_expression[,C Index_Type]])</syntax>
   <arguments>
     <argument>
         <name>Tagname</name>
         <description>Character</description>
     </argument>
     <argument>
         <name>Order_expression</name>
         <description>Character</description>
     </argument>
     <argument>
         <name>Filter_expression</name>
         <description>Character</description>
     </argument>
     <argument>
         <name>Index_Type</name>
         <description>Character</description>
     </argument>
   </arguments>
   <description>Add the next tag.</description>
   <discussion>
      ...put discussion here....
   </discussion>
   <example><![CDATA[
...put example code here....
]]></example>
   <sections>
       <section>
            <description>put description here</description>
            <example><![CDATA[
      ...put example here....
]]></example>
       </section>
   </sections>
   <see>
      <ref>Table Functions and  Methods</ref>
      <ref>Index  Functions and Methods</ref>
      <ref>Writing  Expressions</ref>
   </see>
   <original>.INDEX_ADD() 
Syntax
.INDEX_ADD( Index_Name as C, Order_Expression as C [, Filter_Expression as C [, Index_Type as C ]] )
ArgumentDescription
Index_NameThe name of an existing index.
Order_ExpressionA character  order expression that sorts selected records.
Filter_ExpressionOptional. Default = all entries. A character  filter expression that selects index entries.
Index_TypeOptional. Default = "" (Ascending, not unique). A string of character flags that can specify a descending sort order.

"D" = descending

"U" = unique key values

"DU" = unique descending 

Description
The .INDEX_ADD() method adds another index tag to the index file created by TABLE.INDEX_CREATE_BEGIN() .
Note : There are higher level functions that may be easier to use. See GET_INDEX_DEFINITIONS(), INDEXES_MATCH_DEFSTRING(), and CREATE_INDEXES().
Example
Create a new production index file with a first name and a last name index tag.
tbl = table.current()
table.index_create_begin("LASTNAME", "LAST_NAME")
index_add("FIRSTNAME", "FIRST_NAME")
index1 = tbl.index_create_end()

Create a new temporary index file, but explicitly name the index file so that it is not the production index. Filter the index for state = CA, and include unique keys only.
tbl = table.current()
table.index_create_begin("CA customers", "LAST_NAME", "State_prov = 'CA'", "U")
index2 = tbl.index_create_end(a_DB_current_PATH + "temp.cdx")

See Also
Table Functions and  Methods, Index  Functions and Methods, Writing  Expressions</original>
</page>