<page>
   <topic>table.CREATE_CLONE Function</topic>
   <syntax>P Create_Clone(filename[,flags])</syntax>
   <arguments>
     <argument>
         <name>filename</name>
         <description></description>
     </argument>
     <argument>
         <name>flags</name>
         <description></description>
     </argument>
   </arguments>
   <description>Create an empty copy of the table.</description>
   <discussion>
      ...put discussion here....
   </discussion>
   <example><![CDATA[
...put example code here....
]]></example>
   <sections>
       <section>
            <description>put description here</description>
            <example><![CDATA[
      ...put example here....
]]></example>
       </section>
   </sections>
   <see>
      <ref>Table Functions and  Methods</ref>
      <ref><TBL>.COPY()</ref>
      <ref>TABLE.CREATE_BEGIN()</ref>
      <ref>TABLE.CREATE_END()</ref>
      <ref><TBL>.RECORD_CLONE()</ref>
   </see>
   <original><![CDATA[&ltTBL>.CREATE_CLONE()
Syntax
&ltTABLE_2> as P = &ltTBL>.CREATE_CLONE( Filename as C [, Open_Mode as N ] )
ArgumentDescription
FilenameThe name of the new table.
Open_ModeOptional. Determines the access privileges that apply to the new table file. It uses the following system variables:
VariableFunction
FILE_RO_EXCLUSIVERead Only (Exclusive)
FILE_RW_EXCLUSIVERead or Write (Exclusive)
FILE_RO_SHAREDRead Only (Shared)
FILE_RW_SHAREDRead or Write (Shared)

Description
The &ltTBL>.CREATE_CLONE() method creates a new empty table that matches the structure of an existing table. The &ltTBL>.CREATE_CLONE() method is useful for creating a new table in which records are to be added. It can also be used with the <TBL>.RECORD_CLONE() method to copy records from one table to another.
&ltTBL> is the pointer to the existing table that you want to clone. Filename is the name of the new table. &ltTABLE_2> is the pointer to the new table created by this method. This table is a clone of &ltTBL>.
Note : The &ltTBL>.CREATE_CLONE() method will not copy the source table's field rules or its records. To copy field rules and/or copy records, use the <TBL>.COPY() or <TBL>.DUPLICATE() methods.
Example
This script copies the structure of the current table to a new table, and then copies all the currently selected records in the current table.
dim tbl_target as P
dim tbl_source as P
dim filename as C
tbl_source = table.current()
filename = ui_get_file("Copy To:", "Tables (*.DBF)", "", "N")
if filename = "" then
    end
end if
tbl_target = tbl_source.create_clone(filename)
tbl_source.fetch_first()
while .NOT. tbl_source.fetch_EOF()
    tbl_target.enter_begin()
    tbl_target.record_clone(tbl_source)
    tbl_target.enter_end(.T.)
    tbl_source.fetch_next()
end while
tbl_target.close()

See Also
Table Functions and  Methods, <TBL>.COPY(), TABLE.CREATE_BEGIN(), TABLE.CREATE_END(), <TBL>.RECORD_CLONE()]]></original>
</page>