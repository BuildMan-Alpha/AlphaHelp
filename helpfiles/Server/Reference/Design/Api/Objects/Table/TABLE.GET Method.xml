<page>
   <topic>TABLE.GET Function</topic>
   <syntax>P Get(C table_name)</syntax>
   <arguments>
     <argument>
         <name>table_name</name>
         <description>Character</description>
     </argument>
   </arguments>
   <description>Returns table of name.</description>
   <discussion>
      ...put discussion here....
   </discussion>
   <example><![CDATA[
...put example code here....
]]></example>
   <sections>
       <section>
            <description>put description here</description>
            <example><![CDATA[
      ...put example here....
]]></example>
       </section>
   </sections>
   <see>
      <ref>Table Functions and  Methods</ref>
   </see>
   <original><![CDATA[TABLE.GET()
Syntax
 as P = TABLE.GET( Alias as C )
ArgumentDescription
AliasThe alias name of the table. The alias is the same as the table name (excluding drive and path and file extension) for the first instance of that table that is opened in a session. However, if more than one instance of the a table is opened, Alpha Anywhere will automatically assign each instance a unique Alias.

Description
The TABLE.GET() method takes the Alias of an open table and returns an object pointer to this table.
Use this method when you know the alias of a open table, but you do not have a pointer to it. You need a pointer to a table to use any of the methods to manipulate a table. To obtain a pointer to a table when you do not know the alias name, use the TABLE.CURRENT() method.
Note : If you run this code in the Interactive window, you will first have to select Interactive > Set Interactive Window Session, pick the current window name, and click OK.
Example
Obtain a pointer for the specified table alias.
alias_name = ui_get_list("Choose Table", 1, "customer", "product", "vendor")
if alias_name = "" then
    end
end if
tbl = table.get(alias_name)

See Also
Table Functions and  Methods]]></original>
</page>