<page>
   <topic>SQL::IndexInfo AddColumn Function Function</topic>
   <syntax>L AddColumn(Column as SQL::IndexColumnInfo)</syntax>
   <arguments>
     <argument>
         <name>Column</name>
         <description>SQL::IndexColumnInfo</description>
     </argument>
   </arguments>
   <description>Add a Column.</description>
   <discussion>
      ...put discussion here....
   </discussion>
   <example><![CDATA[
...put example code here....
]]></example>
   <sections>
       <section>
            <description>put description here</description>
            <example><![CDATA[
      ...put example here....
]]></example>
       </section>
   </sections>
   <see>
      <ref>SQL::IndexInfo Object</ref>
      <ref>Limitations</ref>
   </see>
   <original><![CDATA[ SQL::IndexInfo::AddColumn()
Syntax
Result_Flag as L = &ltSQL::IndexInfo>.AddColumn(  &ltIndexColumn> as SQL::IndexColumnInfo  )
ArgumentDescription
Result_FlagTRUE (.T.) if the operation was successful; otherwise  FALSE (.F.).
&ltSQL::IndexInfo>A SQL::IndexInfo object and having (at a minimum) a value for the .name property.
&ltIndexColumn>A SQL::IndexColumnInfo object and having (at a minimum) a value for the .name property.

Description
The AddColumn() method adds a new column definition to an index definition.
Example
dim IndexInfo as SQL::IndexInfo
dim IndexColumn as SQL::IndexColumnInfo

The .Name properties of the SQL::IndexInfo and SQL::IndexColumnInfo objects are required  before calling the AddColumn() method.
IndexInfo.Name = "Primary_Key"
IndexInfo.PrimaryKey = .t.
IndexColumn.Name = "MyColumnName"
if .not. IndexInfo.AddColumn(IndexColumn) then
    ui_msg_box("Error",  "Could not add index")
end if
ui_msg_box("Index Column Definition", IndexInfo.XML)

See Also
SQL::IndexInfo Object
Limitations]]></original>
</page>