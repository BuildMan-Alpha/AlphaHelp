<page>
<group>JSON Functions</group>
<topic>json_generate()</topic>
<syntax>json_generate(* var [,l flagspecialtags [,l flagcondense [,c indent [,l flaghonornulls [,* opsin ]]]]])</syntax>

<arguments>
<argument>
	<name>var</name>
	<description>pointer to variable.</description>	
</argument>
</arguments>

<description>
Generate takes a variable, an Xbasic dot variable, and returns JSON. However the JSON spec does not require this and so single quotes are no longer escaped. This function should be used in place of varToJSON() which will be deprecated. The json_generate() function is identical to varToJSON() in all respects, except that the attribute name and string values are quoted using double quotes in accordance with the official JSON specification. NOTE: Previously the json_generate() function would escape single quotes.
</description>

<example>
	dim pj as p
pj.text = "some text with ' single quote"
?json_generate(pj)
= {
       "text": "some text with ' single quote"
}
</example>

<see>
	<ref>vartojson()</ref>
	<ref>json_parse()</ref>
	<ref>JSON Data</ref>
</see>
</page>