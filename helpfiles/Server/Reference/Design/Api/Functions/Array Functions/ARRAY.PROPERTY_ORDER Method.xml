<page>
   <topic>ARRAY.PROPERTY_ORDER Function</topic>
   <syntax>Array_Order as C = property_order( field as c , values as c )</syntax>
   <arguments>
   
    <argument>
         <name>Array_Order
</name>
         <description>The reordered sequence of array elements.
</description>
     </argument>
     <argument>
         <name>field</name>
         <description>
 
The name of the property to use for reordering.
 </description>
     </argument>
     <argument>
         <name> values</name>
         <description>The values of the property to use for reordering.
</description>
     </argument>
   </arguments>
   <description>Get Property array order list.</description>
  <discussion><![CDATA[The <ARRAY>.PROPERTY_ORDER() method creates a list containing the sequence of reordered array elements. Use the <ARRAY>.REORDER() method to resequence the array.]]></discussion>
  
<sections><section><title>Example</title>
<description>Create an array named arr and populate it with name and desc properties.</description>
<example><![CDATA[ 
dim data as C
data  = <<%str% 
 One|first one 
 Two|second one 
 Three|third one 
 %str%
dim arr[3] as P
arr.initialize_properties("name|desc", data)
? arr
= +[1].
+[2].
+[3].
]]></example>
  </section>
  
<section>
<description>Define an arbitrary order for the name property in the array.</description>
<example><![CDATA[ 
dim refList as C
refList = comma_to_crlf("Two,One,Three") 
 ? reflist
= Two
One
Three
]]></example>
  </section>
  
<section>
<description>Create a string that defines how the array should be reordered.</description>
<example><![CDATA[ 
ord = arr.property_order("name",refList) 
 ? ord 
 = 2 
 1 
 3
]]></example>
  </section>
  
  
<section>
<description>Now sort the array based on this new order.</description>
<example><![CDATA[ 
arr.reorder(ord) 
 ? arr.dump_properties("name|desc") 
 = Two|second one 
 One|first one 
 Three|third one
]]></example>
  </section>
  
  
  </sections>
  
  
  
  
  
  
  
  
  
  
  
   <see>
      <ref>Array Functions and  Methods</ref>
   </see>
   
</page>