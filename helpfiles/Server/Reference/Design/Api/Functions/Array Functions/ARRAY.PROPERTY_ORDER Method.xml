<page>
   <topic>ARRAY.PROPERTY_ORDER Function</topic>
   <syntax>C property_order( field as c , values as c )</syntax>
   <arguments>
     <argument>
         <name>field</name>
         <description>Character</description>
     </argument>
     <argument>
         <name> values</name>
         <description>Character</description>
     </argument>
   </arguments>
   <description>Get Property array order list.</description>
   <discussion>
      ...put discussion here....
   </discussion>
   <example><![CDATA[
...put example code here....
]]></example>
   <sections>
       <section>
            <description>put description here</description>
            <example><![CDATA[
      ...put example here....
]]></example>
       </section>
   </sections>
   <see>
      <ref>Array Functions and  Methods</ref>
   </see>
   <original><![CDATA[&ltARRAY>.PROPERTY_ORDER()
Syntax
Array_Order as C = &ltARRAY>.PROPERTY_ORDER( Field as C, Values as C )
ArgumentDescription
Array_OrderThe reordered sequence of array elements.
FieldThe name of the property to use for reordering.
ValuesThe values of the property to use for reordering.

Description
The &ltARRAY>.PROPERTY_ORDER() method creates a list containing the sequence of reordered array elements. Use the <ARRAY>.REORDER() method to resequence the array.
Examples
Create an array named arr and populate it with name and desc properties.
dim data as C
data  = &lt
One|first one 
Two|second one 
Three|third one 
%str%

dim arr[3] as P
arr.initialize_properties("name|desc", data)
? arr
= +[1].
+[2].
+[3].

Define an arbitrary order for the name property in the array.
dim refList as C
 refList = comma_to_crlf("Two,One,Three") 
? reflist

= Two
One
Three

Create a string that defines how the array should be reordered.
ord = arr.property_order("name",refList) 
? ord 
= 2 
1 
3


Now sort the array based on this new order.
arr.reorder(ord) 

? arr.dump_properties("name|desc") 
= Two|second one 
One|first one 
Three|third one


See Also
Array Functions and  Methods]]></original>
</page>