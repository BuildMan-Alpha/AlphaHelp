<page>
   <topic>ARRAY.INITIALIZE Function</topic>
   <syntax>V initialize(C data[,L append])</syntax>
   <arguments>
     <argument>
         <name>data</name>
         <description>Character</description>
     </argument>
     <argument>
         <name>append</name>
         <description>Logical</description>
     </argument>
   </arguments>
   <description>Initialize the array from a string of newline separated entries.</description>
   <discussion>
      ...put discussion here....
   </discussion>
   <example><![CDATA[
...put example code here....
]]></example>
   <sections>
       <section>
            <description>put description here</description>
            <example><![CDATA[
      ...put example here....
]]></example>
       </section>
   </sections>
   <see>
      <ref>Array Functions and  Methods</ref>
   </see>
   <original><![CDATA[&ltARRAY>.INITIALIZE()
Syntax
&ltARRAY>.INITIALIZE( Data_String as C )
ArgumentDescription
Data_StringThe values to use to initialize the array elements.

Description
The &ltARRAY>.INITIALIZE() method populates an array with values contained in a CR-LF delimited character list.
For example, assume that the string mydata contains this:
"blue
green
red
purple"

The following commands will dimension a new array and initialize the first four array elements:
dim colors[10] as C
Colors.initialize(mydata)

Example
The following script populates an array with the names of the tables in the current database. The A5.TABLE_ENUM() method returns a CR-LF delimited list of all of the tables in a database when called with no argument.
dim tablelist[100] as C
tablelist.initialize(:a5.table_enum())

This script creates and populates an array with 5 elements. The &lt
dim colors[5] as C
colors.initialize(&lt
blue
red
green
yellow
pink
%list%)

The following script populates an array with the first and last names of people in Massachusetts from the Customer table.
dim names[100] as C
list = table.external_record_content_GET("customer", "alltrim(firstname) + ' ' + lastname", "lastname", "state = 'ma'")
names.initialize(list)

See Also
Array Functions and  Methods]]></original>
</page>