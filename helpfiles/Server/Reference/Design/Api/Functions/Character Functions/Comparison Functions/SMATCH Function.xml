<page>
   <topic>SMATCH Function</topic>
   <syntax>Match_Flag as L = SMATCH(C string_1,C pattern)</syntax>
   <arguments>
     <argument>
         <name>string_1</name>
         <description>The character string to compare to the pattern. Character</description>
     </argument>
     <argument>
         <name>pattern</name>
         <description>A character string.</description>
     </argument>
   </arguments>
   <description>Returns TRUE if the string matches the pattern (which can include wildcards).</description>
   <discussion>
Returns .T. (TRUE) if Find_String matches Target_String.
Either parameter can be a constant, character variable, or an expression that returns a character value. SMATCH() returns TRUE only if the entire Find_String matches the Target_String.
SMATCH() is useful for selecting records using query selection criteria, filter expressions, or conditional expressions.
 Note : Either character string parameter can include a Memo field. However, only the first 1024 characters of the Memo field are searched. Use SMATCH_MEMO() to search an entire Memo field.
   </discussion>
   <example><![CDATA[
? smatch("alpha ", "alpha*")
= .T.
? smatch("alpha ", "alpha")
= .F.
? smatch("Beta", "beta")
= .T.
? smatch("alpha ", "alpha  ")
= .F.
]]></example>
   <sections>
       <section>
            <description>put description here</description>
            <example><![CDATA[
      ...put example here....
]]></example>
       </section>
   </sections>
   <see>
      <ref>Character Comparison Functions</ref>
      <ref>Character Search Functions</ref>
   </see>
</page>