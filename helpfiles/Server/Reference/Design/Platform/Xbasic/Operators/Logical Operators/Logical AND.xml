<page>
	<topic>Logical .AND.</topic>
	<description>When a logical AND (.AND.) is placed between two logical expressions that evaluate to logical values, the result is TRUE only if the logical expressions on both sides of the .AND. are TRUE. This is a bitwise logical operation. The following truth table shows all the possible outcomes of an .AND. operation between two expressions that evaluate to logical values:
</description>
<syntax>Result as L = Expression1 .AND. Expression2</syntax>	

<sections>
	<section>
		<title>Using .AND.</title>
		<cases>
			<case>
				<description>If Expression1 is FALSE .AND. Expression2 is FALSE then the result is FALSE</description>
			</case>
			<case>
				<description>If Expression1 is FALSE .AND. Expression2 is TRUE then the result is FALSE</description>
			</case>
			<case>
				<description>If Expression1 is TRUE .AND. Expression2 is FALSE then the result is FALSE</description>
			</case>
			<case>
				<description>If Expression1 is TRUE .AND. Expression2 is TRUE then the result is TRUE</description>
			</case>
			
		</cases>
		
		
	</section>
	
	
	
</sections>


	

<sections>
	<section>
		<title>Example</title>
		<description>For example, if Sales contains 2000 and State is "MA":
</description>
		<example><![CDATA[? (SALES > 1000) .AND. (STATE = "MA")
= TRUE
 ]]></example>	
	</section>
	<section>
		
		<description><![CDATA[<BLOB>.AND() ]]></description>
	</section>
	
</sections>



	
<see>
	<ref></ref>
	
</see>
	
</page>