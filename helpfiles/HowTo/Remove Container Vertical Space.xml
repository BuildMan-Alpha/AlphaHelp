<page>
    <topic>How to Eliminate Vertical Space when Controls are Hidden in a UX Component</topic>
    <description>When a show/hide client-side expression hides a container in a UX Component, some vertical whitespace may still appear on the page. It can be eliminated by using the "Prevent container float" property.</description>
    <discussion>
        <p>The default layout generated for containers uses the float property. Because float is used, this results in extra whitespace when a container is hidden by a client-side expression. Whitespace can be eliminated by turning on "Prevent container float". This changes how the HTML is generated for the container.</p>
        <p>If you want to display several containers next to each other, however, you cannot use the "Prevent container float" property. Enabling "Prevent container float" precludes the ability to align containers side by side.</p>
    </discussion>
    <videos>
        <video>
            <name>Understanding the 'Prevent Container Float' Property</name>
            <link>http://www.ajaxvideotutorials.com/V11Videos/DialogComponent_ContainerPreventFloat.swf</link>
        </video>
    </videos>
    <terms>containers</terms>
</page>